# Archivos ignorados por defecto (los que ya tienes)
/shelf/
/workspace.xml
__pycache__/

# === Python ===
# Ignora la carpeta de bytecode generada automáticamente

# Ignora los archivos de bytecode compilado (*.pyc)
*.pyc
*.pyo

# Entorno virtual (reemplaza 'venv' si usas otro nombre)
venv/
env/

# Directorios de distribución y compilación
build/
dist/
*.egg-info/

# Archivos de depuración y registro
*.log
.coverage

# === Archivos de IDE/Editor (PyCharm, VSCode, etc.) ===
# Ignora la carpeta de configuración del IDE (crucial para .idea)
.idea/

# === Otros ===
# Archivos de caché del sistema operativo
.DS_Store # macOS
Thumbs.db # Windows